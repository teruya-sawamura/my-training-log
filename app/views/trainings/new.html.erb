<h1>Training#new</h1>
<%= form_with(model: [@category, @training], local: true) do |f| %>
  <%= f.label :content, '内容' %>
  <%= f.text_field :content %>
  
  <%= f.label :time, '時間' %>
  <%= f.number_field :time %>

  <%= f.submit '新規作成' %>
<% end %>
